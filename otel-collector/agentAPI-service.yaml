apiVersion: v1
kind: Service
metadata:
  name: agent-api-service
  namespace: monitoring
spec:
  selector:
    app: agent-api
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP